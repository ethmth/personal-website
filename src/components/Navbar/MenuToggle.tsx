/*
import React from "react";
import { Box } from "@chakra-ui/react";

const MenuToggle: React.FC = (toggle, isOpen) => {
  return (
    <Box display={{ base: "block", md: "none" }} onClick={toggle}>
      {isOpen ? "hi sexy" : "hi ugly"}
    </Box>
  );
};

export default MenuToggle;
*/
