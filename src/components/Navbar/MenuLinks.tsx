/*
import { Text, Link } from "@chakra-ui/react";

const MenuItem: React.FC = {(children: React.FC, isLast, to = "/", ...rest)} => {
  return (
    <Link href={to}>
      <Text display="block" {...rest}>
        {children}
      </Text>
    </Link>
  );
};

export default MenuItem;
*/
